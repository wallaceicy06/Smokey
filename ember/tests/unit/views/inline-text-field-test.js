import { moduleFor, test } from 'ember-qunit';

moduleFor('view:inline-text-field', 'Unit | View | inline text field');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
